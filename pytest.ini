[pytest]
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = test_*.py
testpaths = books/tests
addopts = --strict-markers
markers =
    unit: fast tests with no database access
    integration: tests that hit the database and/or the API through the client
